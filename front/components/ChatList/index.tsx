import React from 'react';
import { ChatZone, Section } from '@components/ChatList/styled';

const ChatList = () => {
  return (
    <ChatZone>
      <Section>chatList</Section>
    </ChatZone>
  );
};

export default ChatList;
